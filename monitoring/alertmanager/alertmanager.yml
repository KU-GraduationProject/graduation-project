global:
  resolve_timeout: 5m

route:
  receiver: aiops-webhook
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 1h

receivers:
  - name: aiops-webhook
    webhook_configs:
      - url: http://pipeline:8000/webhook/alert
        send_resolved: true


